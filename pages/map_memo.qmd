---
title: "HRL restoration project map: implicated infrastructure decisions"
author: "Lucy Andrews"
date: "April 2026"
date-format: "MMMM YYYY"
---

::: {.callout-warning}
## Working memo
This is an internal planning document, not a policy or design specification.
:::

## The task

We want to compile a spatial dataset of all HRL restoration projects in the
Sacramento River and Bay-Delta watershed (with relevant attributes), store it in
the HRL data infrastructure, and publish it as an interactive map that staff,
the State Water Resources Control Board, and the public can use.

This is a well-scoped, achievable task and a good one to tackle early because it
is relatively narrow, has a specific end-product, and will prompt engagement
with technical questions that the HRL program needs to start addressing in
preparation for implementation. Several of the infrastructure decisions this
memo originally identified have now been captured as Architecture Decision
Records (ADRs). This memo therefore distinguishes decisions that have been made
from decisions that remain open.

For a thorough discussion of HRL data infrastructure, including decisions that 
need to be made in order for work to progress, please see the
[HRL data infrastructure proposal](hrl_infrastructure_proposal.qmd) page.

---

## Why this task touches so much of the infrastructure

The HRL data infrastructure is designed in four layers, each with a distinct job:

| Layer | Job |
|---|---|
| 1 — Source & Publication | Data is created and archived in a stable, open location |
| 2 — Ingestion & Standardization | Automated pipelines pull the data in, validate it, and put it in a standard format |
| 3 — Storage & Serving | The processed data lives in a central store that other tools can read from |
| 4 — Access & Applications | Program staff and the public interact with the data through maps, packages, and dashboards |

Making a restoration project map means doing something at every one of these
layers. That is what makes this task a useful test case: it forces us to make foundational
decisions in a low-stakes context, before we have a large catalog of datasets
depending on those choices.

---

## Layer by layer: current decision status

### Layer 1 — creating and publishing the dataset

**What this means here:** The restoration project spatial dataset does not exist
yet as a program-administered dataset. It needs to be created from existing GIS
layers, partner data, and/or first-principles digitizing; assigned a stable home;
and given an explicit process for review, update, and versioning.

**Current decision: This dataset is managed through HRL program infrastructure,
not the default EDI-first publication path.**

Our standard process requires that all HRL data be published to EDI before it
enters the infrastructure. EDI is designed for ecological field data with rich,
standardized metadata.

A spatial reference layer such as restoration project boundaries is an accepted
exception to the default path because it needs to function as an evolving shared
program dataset rather than a static ecological archive package. This does not
weaken the EDI-first publication contract for ordinary HRL datasets; it defines
one managed pathway for data products whose update pattern or technical form
does not fit EDI well.

**Current decision: The restoration project spatial dataset is a shared HRL
program dataset with DWR as steward.**

The dataset should be treated as an HRL program asset, not simply as a DWR-owned
GIS layer. DWR stewardship means DWR is responsible for maintaining the technical
system and coordinating the data-management process. It does not mean DWR has
exclusive authority over the scientific or programmatic content. The detailed
update workflow, review cadence, and approval path still need to be defined.

---

### Layer 2 — ingesting and standardizing the data

**What this means here:** Once the data are archived, an automated pipeline needs
to pull them in, validate them (do all features have the required attributes? are
coordinates in the expected projection? etc.), and convert it to the standard
format for storage.

**Current decision: Use standardized export files alongside the database.**

PostgreSQL/PostGIS is the authoritative operational store for standardized
restoration project spatial data. Standardized export files should also be
published for users and systems that need file-based access. Expected export
formats include GeoPackage, GeoJSON, CSV, and GeoParquet, with the exact public
access pattern refined as the map and API architecture are defined.

**Current decision: Use LinkML as the schema source of truth.**

Before the pipeline can validate the data, HRL needs a schema: what fields each
record has, which fields are required, which controlled vocabularies are valid,
and how geometry and coordinate reference system requirements are represented.
That schema should be maintained in LinkML, with downstream validation artifacts,
data dictionaries, and templates generated from or synchronized with the LinkML
source where practical.

**Current decision: Standardize restoration spatial data to EPSG:3310.**

Submitted data may arrive in different coordinate reference systems, but the
standardized restoration project dataset should use EPSG:3310. This gives the
pipeline a clear CRS target for validation, transformation, and downstream
serving.

---

### Layer 3 — storing and serving the data

**What this means here:** The processed spatial dataset needs to live somewhere
permanent that other tools---mapping clients, a data access R package or Python
library, and future analyses---can reliably read from. Storage alone is not
enough; we also need to decide how clients discover, query, and retrieve the
data.

**Current proposal: Use Azure Storage / ADLS Gen2 as the object storage
backbone.**

The ADR inventory now proposes Azure Storage / ADLS Gen2 as the raw,
intermediate, export, and archival object storage backbone. The restoration
project spatial dataset could be the first dataset stored there, which means the
team still needs provisioning, permissions, and naming conventions before the
pipeline can operate.

This is a dependency, not a blocker we can work around: there is no durable
infrastructure location for raw submissions, validation reports, standardized
exports, or archived snapshots until storage exists.

**Current decision: Use PostgreSQL/PostGIS as the authoritative operational
store.**

PostgreSQL/PostGIS is the accepted operational store for standardized spatial
data. The database should hold the current authoritative version used by APIs,
applications, and query workflows. Export files remain important, but they do
not replace the database as the operational source of truth.

**Open decision: What data-serving interface do clients (tools or applications) use, and how is that interface made public?**

The interactive map (and any future applications or end users) needs a stable
way to access the data. A small first version could read a static GeoJSON,
GeoPackage, or GeoParquet file directly from public object storage. That is
simple, but it becomes limiting if clients need to query by geography, filter by
attributes, request only a subset of records, or support multiple applications
without duplicating data access logic.

If those needs are in scope, we likely need a queryable serving layer, such as a
REST API, OGC API Features endpoint, STAC/catalog-backed service, ArcGIS Feature
Layer, or lightweight database-backed API. This is the interface that clients
(tools or applications) would use to access the restoration project dataset,
while the underlying storage remains the durable system of record.

Public access should be treated as the default for this dataset and for the map
that uses it. The decision is therefore not only which interface to use, but how
that interface is made reliably public: stable URLs, clear documentation, a
versioning/update policy, appropriate cross-origin access for web maps, and no
requirement that public users or partner agencies sign in with DWR-specific
accounts. If any subset of the data needs restricted access later, that should
be handled explicitly as an exception rather than as the default design.

---

### Layer 4 — the interactive map

**What this means here:** The map is the deliverable the stakeholder can see,
but "interactive map" can mean several very different things, with different
implications for hosting, maintenance, user experience, and cost.

**Open decision: What does "interactive map" mean for this task?**

The options, roughly in order of complexity:

| Option | What it is | Hosting | Maintenance |
|---|---|---|---|
| Standalone web map | A simple web page with a Leaflet or Mapbox map, hosted publicly | GitHub Pages or cloud hosting | Low; maintenance can be shared among agencies |
| Custom JavaScript web map | A custom Node.js + React, kepler.gl, MapLibre, or similar map with richer client-side interaction | Static hosting for a bundled app, or cloud app hosting if server-side features are needed | Medium; simpler to host than Shiny/Streamlit if static, but requires JavaScript build tooling and frontend maintenance |
| ArcGIS Online map | A hosted Esri web map built from a feature layer or static spatial data product | ArcGIS Online under a DWR or HRL program account | Medium; no server to maintain, but requires account and licensing; maintenance may be challenging to share among agencies |
| Shiny/Streamlit dashboard | A full interactive app with filters, data tables, and map | Posit Connect cloud or other app hosting | Higher; easier for R/Python developers to build, but requires a running server and app-hosting maintenance; maintenance can be shared among agencies |

For a restoration project reference layer, a static or simple embedded map is
probably sufficient and keeps infrastructure costs and complexity low. A full
dashboard becomes worthwhile when users need to filter, query, or download
subsets of the data. We should align on which option is in scope before building
anything.

**Open decision: Who is the audience, and how do we build for their needs?**

The map will be built predominantly for the SWRCB, but there will be other 
audiences that could be served as well. What are all of these audiences' needs, 
what UX/UI design and beta testing practices do we want to engage, and how can
we maximize accessibility?

---

## Summary of decision status

The following table summarizes the current state of decisions needed for the
restoration project map and supporting pipeline.

| Decision | Status | Blocking? |
|---|---|---|
| Does this dataset go through EDI or program infrastructure? | Accepted: managed through HRL program infrastructure as a shared program dataset | No |
| How are shared program datasets administered? | Partly accepted: DWR steward for restoration spatial data; detailed update workflow still needed | Partly |
| What schema language is authoritative? | Accepted: LinkML | No |
| What CRS is used for standardized restoration spatial data? | Accepted: EPSG:3310 | No |
| What is the operational spatial store? | Accepted: PostgreSQL/PostGIS | No |
| Are standardized export files published? | Accepted: yes, alongside the database | No |
| How do we provision cloud object storage? | Proposed: Azure Storage / ADLS Gen2 | Yes |
| What compute layer runs validation and transformation? | Proposed: Azure Container Apps Jobs | Yes |
| What data-serving interface do clients use? | Candidate ADR needed | Yes |
| What does "interactive map" mean? | Candidate ADR needed | Yes |
| Who are the map audiences and what are their UX needs? | Candidate ADR or product brief needed | Yes |

## Related decisions

- [ADR-004: Require static publication of data before ingestion, with EDI as the default repository](../adr/adr/adr-004-static-publication-before-ingestion.md)
- [ADR-005: Allow Central Data Team-managed publication pathways for large or complex datasets](../adr/adr/adr-005-central-data-team-managed-publication-pathways.md)
- [ADR-009: Use PostgreSQL/PostGIS as the authoritative operational store for standardized spatial data](../adr/adr/adr-009-postgresql-postgis-operational-store.md)
- [ADR-010: Publish standardized export files alongside the authoritative database](../adr/adr/adr-010-standardized-export-files.md)
- [ADR-014: Administer restoration project spatial data as a shared HRL program dataset with DWR as steward](../adr/adr/adr-014-restoration-spatial-data-shared-dataset.md)
- [ADR-016: Use LinkML as the source-of-truth schema language for restoration spatial data](../adr/adr/adr-016-linkml-source-of-truth-schema.md)
- [ADR-017: Standardize restoration spatial data to EPSG:3310](../adr/adr/adr-017-standardize-restoration-spatial-data-epsg-3310.md)
